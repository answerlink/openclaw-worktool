# Changelog

## 0.2.0 - 2026-03-20

- Added WorkTool webhook inbound pipeline with async ACK mode.
- Added WorkTool outbound send API integration via `wework/sendRawMessage`.
- Added `roomType`-first chat type resolution for inbound mapping.
- Added packaging pipeline: `build`, `pack`, `verify:pack`, `release`.
